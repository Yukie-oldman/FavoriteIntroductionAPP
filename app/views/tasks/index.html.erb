<h1><%= current_user.name %>のタスク一覧</h1>
  <%= link_to "タスク新規作成", new_user_task_path(current_user) ,class: "btn btn-primary btn-block btn-signup" %>
    <ul>
      <% @tasks.each do |task| %>
        <li><%= link_to task.name, user_task_path(@user,task) %> </li>
      <% end %>
    </ul>